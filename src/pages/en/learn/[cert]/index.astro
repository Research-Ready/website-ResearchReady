---
import { certs } from '../../../../data/certs/index';
export function getStaticPaths() {
  return certs.map(c => ({ params: { cert: c.id } }));
}
const { cert } = Astro.params;
---
<meta http-equiv="refresh" content={`0;url=/learn/${cert}/`}>
<link rel="canonical" href={`/learn/${cert}/`}>
